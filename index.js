const Gameboard = (() => {
	const gameBoard = [];
	return { gameBoard };
})();

const player = (name) => {
	return { name };
};

const Gameflow = (() => {})();

const playerOne = player('Player 1');
const playerTwo = player('Player 2');
